import React from 'react';
import Collection from '../components/Collection';

const CollectionP = () => {
    return (
        <div>
            <Collection />
        </div>
    );
};

export default CollectionP;
